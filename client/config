# Server config

example() {
	HOST="127.0.0.1"
	OPORT="1234"
	IPORT="1235"
	COMPORT="1236"
}

### SERVER CONFIG README:
# You can add more servers to choose from when starting the client.
# Simply copy the "example" function above, replace "example" with any name
# and fill out all the info. You can have as many as you'd like.
# Then just enter the function name in the client when it prompts you for
# a server name.
###

# User config
NAME="User"
NAME_COLOR="\e[1;34m"	# this is blue. you can google the bash color codes, and drop one in that looks like this one, if you dont like blue.
